const dima = require("./Calculator")
// const [operator, ...args] = process.argv.slice(2);
// const numbers = args.map((el)=> Number(el))

// const result = dima.calc(operator, numbers);
const result = dima.init()
console.log(result);

